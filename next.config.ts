import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'standalone',
  // Enable if you need to serve images from external domains
  // images: {
  //   domains: ['example.com'],
  // },
};

export default nextConfig;
